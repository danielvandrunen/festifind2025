# Docker Setup for FestiFind

This guide explains how to use Docker with the FestiFind project to ensure consistent development and deployment environments.

## Getting Started

To build and run the Docker container for local testing:

```bash
./docker/test-deploy.sh
```
